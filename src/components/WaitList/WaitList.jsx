import React from 'react';
import styles from '../WaitList.modules.scss';

const WaitList = (props) => {
    console.log('wait list test', props)
    return (
        <div>
            <h1>TEST</h1>
        </div>
    )

};

export default WaitList;
